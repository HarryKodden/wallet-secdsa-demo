# Example: walt.id Wallet API registration-defaults.conf snippet
# Copy into a *custom* wallet-api config (after WaltCryptoSecdsa.init()).
# Educational only — pin in config is lab-demo only.

defaultKeyConfig: {
    backend: secdsa
    keyType: secp256r1
    config: {
        baseUrl: "http://host.docker.internal:8080"
        accountId: "citizen-42"
        pin: "424242"
    }
}

defaultDidConfig: {
    method: jwk
}
