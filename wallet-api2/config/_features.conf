enabledFeatures = [
    auth                 # JWT account auth + per-account wallet ownership
    wallet2-persistence  # SQL persistence (Postgres) for wallets + account↔wallet links
]
disabledFeatures = [
]
